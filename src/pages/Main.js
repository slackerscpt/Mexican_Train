const MainPage = () => {

    return (
        <div>
            <header>Welcome to Mexican Train</header>
            <button>Start Game</button>
            <button>Setup Game</button>

        </div>
    )
};

export default MainPage;
